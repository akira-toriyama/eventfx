name: build

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Show toolchain
        run: swiftc --version

      - name: Build (release)
        run: ./build.sh

      - name: Sanity check binary
        run: file bin/eventfx && test -x bin/eventfx

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: eventfx
          path: bin/eventfx
          if-no-files-found: error
